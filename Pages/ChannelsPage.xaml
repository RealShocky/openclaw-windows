<UserControl x:Class="OpenClawGUI.Pages.ChannelsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             Background="#0D1117">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="CHANNELS" FontSize="24" FontWeight="Bold" Foreground="#E6EDF3" Margin="0,0,0,5"/>
            <TextBlock Text="Connect OpenClaw to messaging platforms" FontSize="14" Foreground="#7D8590" Margin="0,0,0,25"/>

            <!-- Channel Cards -->
            <WrapPanel>
                <!-- Discord -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="💬" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="Discord" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="Bot integration" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#5865F2" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT CONFIGURED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="⚙️ Configure" Click="ConfigureDiscord_Click" Background="#21262D" Foreground="#C9D1D9" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Slack -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="📱" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="Slack" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="Workspace integration" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#4A154B" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT CONFIGURED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="⚙️ Configure" Click="ConfigureSlack_Click" Background="#21262D" Foreground="#C9D1D9" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Telegram -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="✈️" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="Telegram" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="Bot integration" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#0088CC" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT CONFIGURED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="⚙️ Configure" Click="ConfigureTelegram_Click" Background="#21262D" Foreground="#C9D1D9" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- WhatsApp -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="📞" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="WhatsApp" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="Business API" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#25D366" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT CONFIGURED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="⚙️ Configure" Click="ConfigureWhatsApp_Click" Background="#21262D" Foreground="#C9D1D9" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Signal -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="🔒" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="Signal" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="Secure messaging" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#3A76F0" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT CONFIGURED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="⚙️ Configure" Click="ConfigureSignal_Click" Background="#21262D" Foreground="#C9D1D9" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- iMessage -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="🍎" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="iMessage" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="macOS only" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#7D8590" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT AVAILABLE" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="ℹ️ Requires macOS" Background="#21262D" Foreground="#7D8590" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch" IsEnabled="False"/>
                    </StackPanel>
                </Border>

                <!-- Nostr -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="⚡" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="Nostr" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="Decentralized protocol" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#8B5CF6" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT CONFIGURED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="⚙️ Configure" Click="ConfigureNostr_Click" Background="#21262D" Foreground="#C9D1D9" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Google Chat -->
                <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" 
                        Padding="20" Margin="0,0,15,15" Width="300">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="🟢" FontSize="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="Google Chat" FontSize="18" FontWeight="Bold" Foreground="#E6EDF3"/>
                                <TextBlock Text="Workspace integration" FontSize="12" Foreground="#7D8590"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Background="#00AC47" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <TextBlock Text="NOT CONFIGURED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                        </Border>
                        <Button Content="⚙️ Configure" Click="ConfigureGoogleChat_Click" Background="#21262D" Foreground="#C9D1D9" 
                                BorderBrush="#30363D" BorderThickness="1" Padding="12,8" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
